- en: '3'
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: '3'
- en: Building and Testing Servers
  id: totrans-1
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 构建和测试服务器
- en: In this chapter, we will cover the basics of building and testing servers. We
    will start with a simple server that uses the STDIO transport. The STDIO transport
    is a simple way to communicate with the server using standard input and output
    streams. Building STDIO servers is a great way to get started with the **Model
    Context Protocol** (**MCP**) and understand how it works. It should also be added
    that the STDIO transport is the most common way to communicate with the server,
    and it’s meant for servers running on your machine.
  id: totrans-2
  prefs: []
  type: TYPE_NORMAL
  zh: 在本章中，我们将介绍构建和测试服务器的基础知识。我们将从一个使用 STDIO 传输的简单服务器开始。STDIO 传输是一种通过标准输入和输出流与服务器通信的简单方式。构建
    STDIO 服务器是开始使用 **模型上下文协议**（**MCP**）并了解其工作原理的好方法。还应补充说明，STDIO 传输是与服务器通信的最常见方式，它适用于运行在您的机器上的服务器。
- en: As part of the chapter, we will also cover different ways of testing the server.
    It’s important that what we build works as expected. We will cover different tools
    that can be used visually, in the CLI, and in code.
  id: totrans-3
  prefs: []
  type: TYPE_NORMAL
  zh: 作为本章的一部分，我们还将介绍不同的服务器测试方法。我们构建的内容能够按预期工作是很重要的。我们将介绍可以用于视觉、CLI 和代码中的不同工具。
- en: 'In this chapter, you will learn how to do the following:'
  id: totrans-4
  prefs: []
  type: TYPE_NORMAL
  zh: 在本章中，你将学习以下内容：
- en: Build a simple server using the STDIO transport
  id: totrans-5
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 使用 STDIO 传输构建一个简单的服务器
- en: Describe the core concepts of the server
  id: totrans-6
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 描述服务器的核心概念
- en: Test the server using different tools
  id: totrans-7
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 使用不同的工具测试服务器
- en: 'The chapter covers the following topics:'
  id: totrans-8
  prefs: []
  type: TYPE_NORMAL
  zh: 本章涵盖了以下主题：
- en: A STDIO server
  id: totrans-9
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 一个 STDIO 服务器
- en: Concepts
  id: totrans-10
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 概念
- en: Runtimes
  id: totrans-11
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 运行时
- en: Testing the server
  id: totrans-12
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 测试服务器
- en: First server
  id: totrans-13
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 第一个服务器
- en: A STDIO server
  id: totrans-14
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 一个 STDIO 服务器
- en: Using STDIO transport means that the server communicates with the client through
    standard input and output streams. This means that the server can read input from
    the client via **standard input** (**stdin**) and send output back to the client
    via **standard output** (**stdout**). Okay, that sounds simple, but how does it
    work?
  id: totrans-15
  prefs: []
  type: TYPE_NORMAL
  zh: 使用 STDIO 传输意味着服务器通过标准输入和输出流与客户端通信。这意味着服务器可以通过 **标准输入**（**stdin**）从客户端读取输入，并通过
    **标准输出**（**stdout**）将输出发送回客户端。好吧，听起来很简单，但它是如何工作的？
- en: 'MCP uses JSON-RPC 2.0 as its wire format. Thereby, it needs to convert stream
    messages into JSON-RPC format for transmission and convert received JSON-RPC messages
    back into stream messages. Imagine the following example in the terminal:'
  id: totrans-16
  prefs: []
  type: TYPE_NORMAL
  zh: MCP 使用 JSON-RPC 2.0 作为其线格式。因此，它需要将流消息转换为 JSON-RPC 格式进行传输，并将接收到的 JSON-RPC 消息转换回流消息。想象以下在终端中的示例：
- en: '[PRE0]'
  id: totrans-17
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: 'To be used in a STDIO server, the preceding message would be converted into
    JSON-RPC format, and it would look like this:'
  id: totrans-18
  prefs: []
  type: TYPE_NORMAL
  zh: 要在 STDIO 服务器中使用，前面的消息将被转换为 JSON-RPC 格式，看起来像这样：
- en: '[PRE1]'
  id: totrans-19
  prefs: []
  type: TYPE_PRE
  zh: '[PRE1]'
- en: 'That’s an interesting example, but what really matters to you as a developer
    is two things:'
  id: totrans-20
  prefs: []
  type: TYPE_NORMAL
  zh: 这是一个有趣的例子，但作为开发者，真正对你来说重要的是两件事：
- en: How to build a STDIO server. We’ll show this shortly in an upcoming section.
  id: totrans-21
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 如何构建一个 STDIO 服务器。我们将在接下来的部分中简要展示。
- en: Why it matters, and what it means that your server is a STDIO server. It means
    that your server is running on your local machine and is communicating with the
    client through standard input and output streams. In *Chapter 4* and *Chapter
    5*, we’ll show you how to build a server that can communicate with servers over
    the internet.
  id: totrans-22
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 为什么这很重要，以及你的服务器是 STDIO 服务器意味着什么。这意味着你的服务器正在你的本地机器上运行，并通过标准输入和输出流与客户端通信。在第 4
    章和第 5 章中，我们将向你展示如何构建可以与互联网上的服务器通信的服务器。
- en: Concepts
  id: totrans-23
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 概念
- en: Let’s take a look at the core concepts (features, if you will) that the server
    can offer.
  id: totrans-24
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们看看服务器可以提供的一些核心概念（如果你愿意称之为功能）。
- en: Resources
  id: totrans-25
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 资源
- en: 'These are the data and context that the server can provide to the client. The
    way MCP is meant to be used is by the client having an LLM when they communicate
    with the server. Resources in this use case serve as context that could be added
    to the LLM at the time of the prompt. Imagine the following scenario playing out:'
  id: totrans-26
  prefs: []
  type: TYPE_NORMAL
  zh: 这些是服务器可以提供给客户端的数据和上下文。MCP 的使用方式是客户端在与服务器通信时拥有一个 LLM。在这个用例中，资源作为上下文，可以在提示时添加到
    LLM 中。想象以下场景：
- en: '![Figure 3.1 – Resources scenario](img/B34123_03_1.png)'
  id: totrans-27
  prefs: []
  type: TYPE_IMG
  zh: '![图 3.1 – 资源场景](img/B34123_03_1.png)'
- en: Figure 3.1 – Resources scenario
  id: totrans-28
  prefs: []
  type: TYPE_NORMAL
  zh: 图 3.1 – 资源场景
- en: In this scenario, the context ensures that the end user gets a better result
    as the server’s context is paired with the user’s prompt, like a simplified **retrieval-augmented
    generation** (**RAG**) pattern. That is, you pair the user’s prompt with your
    data to get a better response.
  id: totrans-29
  prefs: []
  type: TYPE_NORMAL
  zh: 在这种情况下，上下文确保最终用户获得更好的结果，因为服务器的上下文与用户的提示配对，就像简化的**检索增强生成**（**RAG**）模式。也就是说，你将用户的提示与你的数据配对以获得更好的响应。
- en: 'A specific example implementing this way of thinking could be where the user
    asks for products like so:'
  id: totrans-30
  prefs: []
  type: TYPE_NORMAL
  zh: 一个具体实现这种思维方式的方法示例是用户请求产品如下：
- en: '**Prompt**'
  id: totrans-31
  prefs: []
  type: TYPE_NORMAL
  zh: '**提示**'
- en: '[PRE2]'
  id: totrans-32
  prefs: []
  type: TYPE_PRE
  zh: '[PRE2]'
- en: '![Figure 3.2 – Example of a resource interaction](img/B34123_03_2.png)'
  id: totrans-33
  prefs: []
  type: TYPE_IMG
  zh: '![图3.2 – 资源交互示例](img/B34123_03_2.png)'
- en: Figure 3.2 – Example of a resource interaction
  id: totrans-34
  prefs: []
  type: TYPE_NORMAL
  zh: 图3.2 – 资源交互示例
- en: In this specific product query, we call resources first to learn what table
    to query, and then we let the LLM identify which tool to call. Thanks to calling
    the resource, we gain extra knowledge on what tools to select and what parameters
    to use.
  id: totrans-35
  prefs: []
  type: TYPE_NORMAL
  zh: 在这个特定的产品查询中，我们首先调用资源来了解要查询哪个表，然后我们让LLM确定要调用哪个工具。多亏了调用资源，我们获得了关于选择哪些工具以及使用哪些参数的额外知识。
- en: Resources can also be used in a manner where the LLM isn’t used, but the way
    you should think about your server is that it’s here to empower a client’s LLM.
    That means the tools, resources, and prompts you provide should be helpful.
  id: totrans-36
  prefs: []
  type: TYPE_NORMAL
  zh: 资源也可以在不使用LLM的情况下使用，但你应该这样思考你的服务器：它是为了赋予客户端的LLM能力。这意味着你提供的工具、资源和提示应该是有帮助的。
- en: 'Now that we understand when resources are used, let’s talk more about the nature
    of them. Resources are static and can be anything that the server can access and
    share with the client. What’s important to know is that you can ask for a resource
    either with or without a template. If there’s only one file or one app setting
    configuration, it makes sense to create that with a set name such as `config`:'
  id: totrans-37
  prefs: []
  type: TYPE_NORMAL
  zh: 现在我们已经了解了何时使用资源，让我们更多地讨论它们的本质。资源是静态的，可以是服务器可以访问并与客户端共享的任何内容。重要的是要知道，你可以使用或不用模板来请求资源。如果只有一个文件或一个应用程序设置配置，使用一个设置名称，如
    `config` 是有意义的：
- en: '[PRE3]'
  id: totrans-38
  prefs: []
  type: TYPE_PRE
  zh: '[PRE3]'
- en: Here, we are returning the same type of information each time.
  id: totrans-39
  prefs: []
  type: TYPE_NORMAL
  zh: 在这里，我们每次都返回相同类型的信息。
- en: 'However, if there are many types of settings, such as user settings, calendar
    settings, and so on, it might make sense to create a more templated version such
    as `settings://{type}`:'
  id: totrans-40
  prefs: []
  type: TYPE_NORMAL
  zh: 然而，如果有许多类型的设置，例如用户设置、日历设置等，创建一个更模板化的版本，如 `settings://{type}` 可能是有意义的：
- en: '[PRE4]'
  id: totrans-41
  prefs: []
  type: TYPE_PRE
  zh: '[PRE4]'
- en: The settings are still non-changing, but there are many of them, so we choose
    to group them under one namespace.
  id: totrans-42
  prefs: []
  type: TYPE_NORMAL
  zh: 设置仍然是不可变的，但有很多，所以我们选择将它们分组在一个命名空间下。
- en: In this case, the resource is called `settings` and takes `type` as a parameter.
    For example, `settings://hello` would match this pattern.
  id: totrans-43
  prefs: []
  type: TYPE_NORMAL
  zh: 在这种情况下，资源被命名为 `settings` 并接受 `type` 作为参数。例如，`settings://hello` 将匹配此模式。
- en: Tools
  id: totrans-44
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 工具
- en: 'Tools are the functions or capabilities that the server can perform. Examples
    of tools include data processing functions, tools that call an API to fetch data,
    and more. For tools, we need to define the input and output by providing a schema.
    How this is done looks different depending on the used runtime, but the idea is
    that it should be clear to the consumer of the tool what the input and output
    are. For example, if we have a tool that takes two numbers and returns their product,
    we can define it like this:'
  id: totrans-45
  prefs: []
  type: TYPE_NORMAL
  zh: 工具是服务器可以执行的功能或能力。工具的例子包括数据处理函数、调用API以获取数据的工具等。对于工具，我们需要通过提供模式来定义输入和输出。具体如何操作取决于所使用的运行时，但理念是应该让工具的消费者清楚输入和输出是什么。例如，如果我们有一个接受两个数字并返回其乘积的工具，我们可以这样定义它：
- en: '[PRE5]'
  id: totrans-46
  prefs: []
  type: TYPE_PRE
  zh: '[PRE5]'
- en: In this example, we define a tool called `multiply` that takes two numbers as
    input and returns their product. The input and output are defined using Python
    type hints.
  id: totrans-47
  prefs: []
  type: TYPE_NORMAL
  zh: 在这个例子中，我们定义了一个名为 `multiply` 的工具，它接受两个数字作为输入并返回它们的乘积。输入和输出使用Python类型提示定义。
- en: Prompts
  id: totrans-48
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 提示
- en: 'Prompts are the templated messages or workflows that guide the interactions
    between the client and server. A good example of a prompt is a template that helps
    the client write a product description or slogan in the context of an e-commerce
    application. Just like resources and tools, prompts can take input. Here’s an
    example of a prompt that takes a product name and returns a product description:'
  id: totrans-49
  prefs: []
  type: TYPE_NORMAL
  zh: 提示是模板化的消息或工作流程，用于指导客户端和服务器之间的交互。一个很好的提示例子是帮助客户端在电子商务应用程序的上下文中编写产品描述或口号的模板。就像资源和工具一样，提示也可以接受输入。以下是一个接受产品名称并返回产品描述的提示示例：
- en: '[PRE6]'
  id: totrans-50
  prefs: []
  type: TYPE_PRE
  zh: '[PRE6]'
- en: Here, we define a prompt called `describe_product` that takes a product name
    as input and returns a product description. The input is defined using Python
    type hints.
  id: totrans-51
  prefs: []
  type: TYPE_NORMAL
  zh: 在这里，我们定义了一个名为 `describe_product` 的提示，它接受一个产品名称作为输入并返回产品描述。输入是通过 Python 类型提示定义的。
- en: Now that we know what our server can contain, what else do we need to know?
  id: totrans-52
  prefs: []
  type: TYPE_NORMAL
  zh: 既然我们已经知道了我们的服务器可以包含什么，我们还需要了解什么？
- en: Runtimes
  id: totrans-53
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 运行时
- en: 'The officially supported runtimes at present for MCP are TypeScript, Python,
    .NET, Java and Kotlin, Rust, and Go. More are being added all the time. For an
    updated list of runtimes, please refer to the MCP documentation here: [https://modelcontextprotocol.io/docs/sdk](https://modelcontextprotocol.io/docs/sdk).
    Each of these runtimes has its own SDK you can use. The implementation of the
    runtimes is similar, but there are some differences.'
  id: totrans-54
  prefs: []
  type: TYPE_NORMAL
  zh: 目前官方支持 MCP 的运行时包括 TypeScript、Python、.NET、Java 和 Kotlin、Rust 和 Go。更多运行时正在不断增加。有关运行时的最新列表，请参阅此处：[https://modelcontextprotocol.io/docs/sdk](https://modelcontextprotocol.io/docs/sdk)。每个运行时都有自己的
    SDK 可以使用。运行时的实现类似，但也有一些差异。
- en: Excited? Let’s get started!
  id: totrans-55
  prefs: []
  type: TYPE_NORMAL
  zh: 激动吗？让我们开始吧！
- en: Testing the server
  id: totrans-56
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 测试服务器
- en: There are many tools at your disposal to test the server. Why we test is that
    we want to ensure that the server is working as expected. The tools we will cover
    in this chapter are as follows.
  id: totrans-57
  prefs: []
  type: TYPE_NORMAL
  zh: 你有很多工具可以使用来测试服务器。我们为什么要测试呢？因为我们想确保服务器按预期工作。在本章中我们将涵盖的工具如下。
- en: Inspector
  id: totrans-58
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 检查员
- en: This is a CLI tool that can present both a UI and a CLI interface. The latter
    is meant for scripting and automation.
  id: totrans-59
  prefs: []
  type: TYPE_NORMAL
  zh: 这是一个 CLI 工具，可以提供 UI 和 CLI 界面。后者用于脚本化和自动化。
- en: The inspector tool runs a Node.js package with npx, so ensure that you have
    the Node.js runtime installed. This is also true even if you run the inspector
    tool via a Python command, as Python wraps a call to the underlying Node.js process.
  id: totrans-60
  prefs: []
  type: TYPE_NORMAL
  zh: 检查员工具通过 npx 运行一个 Node.js 包，所以请确保你已经安装了 Node.js 运行时。即使你通过 Python 命令运行检查员工具，这也同样适用，因为
    Python 会包装对底层 Node.js 进程的调用。
- en: The UI is meant for manual testing and debugging. In this example command, we
    run the inspector tool. Ensure that you stand in the same directory as the server
    file when you run the following commands.
  id: totrans-61
  prefs: []
  type: TYPE_NORMAL
  zh: UI 旨在进行手动测试和调试。在这个示例命令中，我们运行了检查员工具。确保你在运行以下命令时站在服务器文件相同的目录中。
- en: 'The Python SDK installed an executable `mcp` that helps run the server:'
  id: totrans-62
  prefs: []
  type: TYPE_NORMAL
  zh: Python SDK 安装了一个名为 `mcp` 的可执行文件，它有助于运行服务器：
- en: '[PRE7]'
  id: totrans-63
  prefs: []
  type: TYPE_PRE
  zh: '[PRE7]'
- en: 'By running this tool, we can test the server in visual mode. Here’s a screenshot
    of the inspector tool:'
  id: totrans-64
  prefs: []
  type: TYPE_NORMAL
  zh: 通过运行此工具，我们可以在视觉模式下测试服务器。以下是检查员工具的截图：
- en: '![Figure 3.3 – Inspector tool](img/B34123_03_3.png)'
  id: totrans-65
  prefs: []
  type: TYPE_IMG
  zh: '![图 3.3 – 检查员工具](img/B34123_03_3.png)'
- en: Figure 3.3 – Inspector tool
  id: totrans-66
  prefs: []
  type: TYPE_NORMAL
  zh: 图 3.3 – 检查员工具
- en: 'Ensure that the visual tool specifies the following fields:'
  id: totrans-67
  prefs: []
  type: TYPE_NORMAL
  zh: 确保视觉工具指定以下字段：
- en: '**Transport Type**: **STDIO**'
  id: totrans-68
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**传输类型**：**STDIO**'
- en: '**Command**: `mcp`'
  id: totrans-69
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**命令**：`mcp`'
- en: '**Arguments**: `run server.py`'
  id: totrans-70
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**参数**：`run server.py`'
- en: 'You can also run the inspector tool in CLI mode. This is useful for scripting
    and automation. You type almost the same command as earlier, but you add the `--cli`
    flag:'
  id: totrans-71
  prefs: []
  type: TYPE_NORMAL
  zh: 你也可以在 CLI 模式下运行检查员工具。这对于脚本化和自动化很有用。你输入的命令几乎和之前一样，但你需要添加 `--cli` 标志：
- en: '[PRE8]'
  id: totrans-72
  prefs: []
  type: TYPE_PRE
  zh: '[PRE8]'
- en: Here, we are adding the `--method` command followed by the `tools/list` argument,
    which says we want to list all tools on the server.
  id: totrans-73
  prefs: []
  type: TYPE_NORMAL
  zh: 这里，我们添加了 `--method` 命令，后面跟着 `tools/list` 参数，表示我们想要列出服务器上的所有工具。
- en: 'Note that `mcp dev` in the Python pivot earlier wraps a Node.js tool, which
    is the inspector. If you want full access to all the features of the inspector,
    you can run it directly for both Node.js and Python, and that’s what we recommend
    doing (that is, run it like so: `npx @modelcontextprotocol/inspector`).'
  id: totrans-74
  prefs: []
  type: TYPE_NORMAL
  zh: 注意，Python 中的 `mcp dev` 包裹了一个 Node.js 工具，即检查员。如果你想完全访问检查员的所有功能，可以直接在 Node.js
    和 Python 中运行它，我们推荐这样做（即，像这样运行：`npx @modelcontextprotocol/inspector`）。
- en: cURL
  id: totrans-75
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: cURL
- en: 'A standard command-line tool such as cURL can be used to send requests to the
    server. This is normally used to test servers using SSE or streamable HTTP as
    transport. Other tools capable of making web requests can be used as well. A `curl`
    command will only work if the server is running on the internet, which is the
    case with SSE servers. For STDIO servers, you need to use the inspector or a custom
    client to send requests to the server. Let’s see a typical `curl` command:'
  id: totrans-76
  prefs: []
  type: TYPE_NORMAL
  zh: 可以使用标准命令行工具如 cURL 向服务器发送请求。这通常用于测试使用 SSE 或可流式传输 HTTP 作为传输的服务器。也可以使用其他能够发送网络请求的工具。只有当服务器在互联网上运行时，`curl`
    命令才会起作用，这对于 SSE 服务器来说是这种情况。对于 STDIO 服务器，你需要使用检查员或自定义客户端来向服务器发送请求。让我们看看一个典型的 `curl`
    命令：
- en: '[PRE9]'
  id: totrans-77
  prefs: []
  type: TYPE_PRE
  zh: '[PRE9]'
- en: In the preceding command, we are sending a `POST` request to the server with
    the `tools/list` method. The server will respond with a list of tools available
    on the server. This is a good alternative to using the inspector in CLI mode.
  id: totrans-78
  prefs: []
  type: TYPE_NORMAL
- en: Tests
  id: totrans-79
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: 'It’s possible to write unit tests for the server. This is a good practice in
    addition to using the inspector. The tests can be run in a CI/CD pipeline and
    are useful to ensure that the server is working as expected. You can use any testing
    framework you like, and you can add resources, tools, and prompts to the server
    and then test them. Let’s see an example of a test:'
  id: totrans-80
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE10]'
  id: totrans-81
  prefs: []
  type: TYPE_PRE
  zh: '[PRE10]'
- en: 'In the preceding example, we do the following:'
  id: totrans-82
  prefs: []
  type: TYPE_NORMAL
- en: Use the `pytest` framework to test the server
  id: totrans-83
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: Create a new server instance and add a tool called `add`
  id: totrans-84
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: Test that the tool is added to the server and that the number of tools is equal
    to `1`
  id: totrans-85
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: This is a simple test, but it shows how you can use `pytest` to test the server.
  id: totrans-86
  prefs: []
  type: TYPE_NORMAL
- en: Now, we have a good grasp of the concepts and features of the server; let’s
    write down the plan for how we will go about building our first server.
  id: totrans-87
  prefs: []
  type: TYPE_NORMAL
- en: First server
  id: totrans-88
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
- en: 'To build our first server, let’s first go through the steps to build a simple
    server:'
  id: totrans-89
  prefs: []
  type: TYPE_NORMAL
- en: '**Create a new project**: We will create a new project with the needed dependencies
    and set up any environment specifics. You’re highly encouraged to create a virtual
    environment to ensure you don’t install libraries globally. By using a virtual
    environment, each project is isolated from other projects and you don’t have to
    worry about conflicting library versions that might otherwise occur.'
  id: totrans-90
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '**Install dependencies**: Here, we will list and install dependencies for the
    project, which differ, of course, depending on the runtime you are using.'
  id: totrans-91
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '**Add server code**: This is where we will add the server’s features. As part
    of this, we will discuss the feature, its input and output, and schemas.'
  id: totrans-92
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '**Test the server with inspector**: Inspector is a tool that helps you ensure
    new features work okay. The tool allows you to run it in both CLI mode and a visual
    mode, where it shows a web browser UI:'
  id: totrans-93
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: The CLI mode is appropriate for CI/CD scenarios, as it responds with JSON in
    a terminal
  id: totrans-94
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
- en: The visual mode is more appropriate when you, as a developer, try to ensure
    that the server is working as it should
  id: totrans-95
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
- en: Let’s write some code!
  id: totrans-96
  prefs: []
  type: TYPE_NORMAL
- en: 'Step 1: Create a new project'
  id: totrans-97
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: 'Before we write any code, we need a project. This will set us up for success.
    The project will contain everything we need to write our server and define tests
    and scripts for testing and running the server:'
  id: totrans-98
  prefs: []
  type: TYPE_NORMAL
- en: 'Create the following folder structure:'
  id: totrans-99
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '[PRE11]'
  id: totrans-100
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE11]'
- en: 'Create a virtual environment:'
  id: totrans-101
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '[PRE12]'
  id: totrans-102
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE12]'
- en: 'Activate the virtual environment:'
  id: totrans-103
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '[PRE13]'
  id: totrans-104
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE13]'
- en: You should see the name of the virtual environment in the terminal prompt. This
    means that the virtual environment is activated, and any packages you install
    will be installed in this environment.
  id: totrans-105
  prefs: []
  type: TYPE_NORMAL
- en: Great, now we have a virtual environment and a folder structure. Next, we need
    to install the dependencies.
  id: totrans-106
  prefs: []
  type: TYPE_NORMAL
- en: 'Step 2: Install dependencies'
  id: totrans-107
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: 'Run the following command in the terminal:'
  id: totrans-108
  prefs: []
  type: TYPE_NORMAL
  zh: 在终端中运行以下命令：
- en: '[PRE14]'
  id: totrans-109
  prefs: []
  type: TYPE_PRE
  zh: '[PRE14]'
- en: This will install the MCP SDK and the CLI tools.
  id: totrans-110
  prefs: []
  type: TYPE_NORMAL
  zh: 这将安装MCP SDK和CLI工具。
- en: 'Step 3: Add server code'
  id: totrans-111
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 第3步：添加服务器代码
- en: 'Add the following code to `server.py`:'
  id: totrans-112
  prefs: []
  type: TYPE_NORMAL
  zh: 将以下代码添加到`server.py`中：
- en: '[PRE15]'
  id: totrans-113
  prefs: []
  type: TYPE_PRE
  zh: '[PRE15]'
- en: 'The preceding code does the following:'
  id: totrans-114
  prefs: []
  type: TYPE_NORMAL
  zh: 上述代码执行以下操作：
- en: Creates an MCP server instance with the name `Demo`
  id: totrans-115
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 创建一个名为`Demo`的MCP服务器实例
- en: Adds a tool called `multiply` that takes two numbers as input and returns their
    sum
  id: totrans-116
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 添加一个名为`multiply`的工具，它接受两个数字作为输入并返回它们的和
- en: Adds a resource called `greeting` that takes a name as input and returns a personalized
    greeting
  id: totrans-117
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 添加一个名为`greeting`的资源，它接受一个名称作为输入并返回个性化的问候
- en: Creates a prompt called `review_code` that takes a code snippet as input and
    returns a review of the code
  id: totrans-118
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 创建一个名为`review_code`的提示，它接受代码片段作为输入并返回代码的审查
- en: 'Step 4: Test the server with the inspector'
  id: totrans-119
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 第4步：使用检查器测试服务器
- en: 'Here, we will use the inspector to test the server. The inspector is a CLI
    tool that can present both a UI and a CLI interface. The latter is meant for scripting
    and automation. The UI is meant for manual testing and debugging:'
  id: totrans-120
  prefs: []
  type: TYPE_NORMAL
  zh: 在这里，我们将使用检查器来测试服务器。检查器是一个CLI工具，可以提供UI和CLI界面。后者用于脚本和自动化。UI用于手动测试和调试：
- en: 'Run the following command in the terminal:'
  id: totrans-121
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
  zh: 在终端中运行以下命令：
- en: '[PRE16]'
  id: totrans-122
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE16]'
- en: This will start the inspector tool on port `6274`.
  id: totrans-123
  prefs: []
  type: TYPE_NORMAL
  zh: 这将在端口`6274`上启动检查器工具。
- en: Navigate to `http://localhost:6274` in your web browser. This should start a
    web server with a visual interface, allowing you to test the sample.
  id: totrans-124
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
  zh: 在您的网络浏览器中导航到`http://localhost:6274`。这应该启动一个带有可视化界面的Web服务器，允许您测试示例。
- en: 'In the visual interface, ensure you fill in the fields like so:'
  id: totrans-125
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
  zh: 在可视化界面中，确保您填写如下字段：
- en: '**Command**: `"mcp"`'
  id: totrans-126
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**命令**：`"mcp"`'
- en: '**Arguments**: `"run server.py"`'
  id: totrans-127
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**参数**：`"run server.py"`'
- en: Click **Connect**.
  id: totrans-128
  prefs: []
  type: TYPE_NORMAL
  zh: 点击**连接**。
- en: 'Select the **Tools** tab and **List Tools**, and **multiply** should be listed.
    Click **multiply** and fill in the fields like so:'
  id: totrans-129
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
  zh: 选择**工具**选项卡和**列出工具**，**multiply**应该被列出。点击**multiply**并填写如下字段：
- en: '**first**: `2`'
  id: totrans-130
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**第一个**：`2`'
- en: '**second**: `4`'
  id: totrans-131
  prefs:
  - PREF_IND
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**第二个**：`4`'
- en: You should see the result **8** in the **Tool Result** field.
  id: totrans-132
  prefs: []
  type: TYPE_NORMAL
  zh: 您应该在**工具结果**字段中看到结果**8**。
- en: Great, now we have a working server, and we can test it using the inspector.
  id: totrans-133
  prefs: []
  type: TYPE_NORMAL
  zh: 太好了，现在我们有一个正在运行的服务器，我们可以使用检查器来测试它。
- en: 'Step 5: Test the server with the inspector in CLI mode'
  id: totrans-134
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 第5步：使用CLI模式下的检查器测试服务器
- en: In this subsection, we will run the inspector directly in CLI mode. The inspector
    is a Node.js app, and `mcp dev` is a wrapper around it.
  id: totrans-135
  prefs: []
  type: TYPE_NORMAL
  zh: 在本小节中，我们将直接在CLI模式下运行检查器。检查器是一个Node.js应用程序，而`mcp dev`是它的包装器。
- en: At the time of this writing, `mcp dev` does not support all the features made
    available in the inspector. We will therefore show how to run the inspector directly
    as a Node.js app as that’s what it’s been written in.
  id: totrans-136
  prefs: []
  type: TYPE_NORMAL
  zh: 在撰写本文时，`mcp dev`不支持检查器提供的所有功能。因此，我们将展示如何直接作为Node.js应用程序运行检查器，因为它是用这种方式编写的。
- en: 'Let’s show some useful commands you can run in the inspector:'
  id: totrans-137
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们展示一些您可以在检查器中运行的实用命令：
- en: '**List tools**: Run the following command in the terminal:'
  id: totrans-138
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**列出工具**：在终端中运行以下命令：'
- en: '[PRE17]'
  id: totrans-139
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE17]'
- en: 'This will list all the tools available on the server, and you should see the
    following output:'
  id: totrans-140
  prefs: []
  type: TYPE_NORMAL
  zh: 这将列出服务器上可用的所有工具，您应该看到以下输出：
- en: '[PRE18]'
  id: totrans-141
  prefs: []
  type: TYPE_PRE
  zh: '[PRE18]'
- en: Here, you see all the tools on the server in JSON format. We only have one tool,
    `multiply`, but we can see that it has an `inputSchema` with `first` and `second`
    parameters.
  id: totrans-142
  prefs: []
  type: TYPE_NORMAL
  zh: 在这里，您可以看到服务器上的所有工具以JSON格式。我们只有一个工具`multiply`，但我们可以看到它有一个`inputSchema`，包含`first`和`second`参数。
- en: '**Call a tool**: Run the following command in the terminal:'
  id: totrans-143
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**调用工具**：在终端中运行以下命令：'
- en: '[PRE19]'
  id: totrans-144
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE19]'
- en: 'You should see a response like this:'
  id: totrans-145
  prefs: []
  type: TYPE_NORMAL
  zh: 您应该看到如下响应：
- en: '[PRE20]'
  id: totrans-146
  prefs: []
  type: TYPE_PRE
  zh: '[PRE20]'
- en: '**List resources**: Run the following command in the terminal:'
  id: totrans-147
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**列出资源**：在终端中运行以下命令：'
- en: '[PRE21]'
  id: totrans-148
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE21]'
- en: 'This will list all the resources available in the server, and you should see
    the following output:'
  id: totrans-149
  prefs: []
  type: TYPE_NORMAL
  zh: 这将列出服务器中可用的所有资源，您应该看到以下输出：
- en: '[PRE22]'
  id: totrans-150
  prefs: []
  type: TYPE_PRE
  zh: '[PRE22]'
- en: 'Empty? The reason is that there’s a difference between a resource and a templated
    resource. Here’s how you can list the templated resources:'
  id: totrans-151
  prefs: []
  type: TYPE_NORMAL
  zh: 空的？原因是资源和模板资源之间有一个区别。以下是列出模板资源的方法：
- en: '[PRE23]'
  id: totrans-152
  prefs: []
  type: TYPE_PRE
  zh: '[PRE23]'
- en: 'You should get a response like this:'
  id: totrans-153
  prefs: []
  type: TYPE_NORMAL
  zh: 您应该得到如下响应：
- en: '[PRE24]'
  id: totrans-154
  prefs: []
  type: TYPE_PRE
  zh: '[PRE24]'
- en: 'Let’s call our templated resource:'
  id: totrans-155
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 让我们调用我们的模板资源：
- en: '[PRE25]'
  id: totrans-156
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE25]'
- en: 'You should see a response like this:'
  id: totrans-157
  prefs: []
  type: TYPE_NORMAL
  zh: 您应该看到如下响应：
- en: '[PRE26]'
  id: totrans-158
  prefs: []
  type: TYPE_PRE
  zh: '[PRE26]'
- en: 'Let’s list prompts next with the following command:'
  id: totrans-159
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 让我们使用以下命令列出提示：
- en: '[PRE27]'
  id: totrans-160
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE27]'
- en: 'You should see an output similar to the following:'
  id: totrans-161
  prefs: []
  type: TYPE_NORMAL
  zh: 您应该看到类似以下输出：
- en: '[PRE28]'
  id: totrans-162
  prefs: []
  type: TYPE_PRE
  zh: '[PRE28]'
- en: 'To call a prompt, we would type the name of the prompt and `prompts/get`, like
    so:'
  id: totrans-163
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 要调用提示，我们将输入提示名称和`prompts/get`，如下所示：
- en: '[PRE29]'
  id: totrans-164
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE29]'
- en: 'You should see a response like this:'
  id: totrans-165
  prefs: []
  type: TYPE_NORMAL
  zh: 您应该看到如下响应：
- en: '[PRE30]'
  id: totrans-166
  prefs: []
  type: TYPE_PRE
  zh: '[PRE30]'
- en: 'Optionally, you could add a resource in `server.py`, like so:'
  id: totrans-167
  prefs: []
  type: TYPE_NORMAL
  zh: 可选地，您可以在`server.py`中添加一个资源，如下所示：
- en: '[PRE31]'
  id: totrans-168
  prefs: []
  type: TYPE_PRE
  zh: '[PRE31]'
- en: 'Read it like so:'
  id: totrans-169
  prefs: []
  type: TYPE_NORMAL
  zh: 请这样阅读：
- en: '[PRE32]'
  id: totrans-170
  prefs: []
  type: TYPE_PRE
  zh: '[PRE32]'
- en: 'You should see a response like this:'
  id: totrans-171
  prefs: []
  type: TYPE_NORMAL
  zh: 您应该看到如下响应：
- en: '[PRE33]'
  id: totrans-172
  prefs: []
  type: TYPE_PRE
  zh: '[PRE33]'
- en: Note how you invoke resources and resource templates in the same way using the
    `uri` parameter.
  id: totrans-173
  prefs: []
  type: TYPE_NORMAL
  zh: 注意您如何使用`uri`参数以相同的方式调用资源和资源模板。
- en: Summary
  id: totrans-174
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 摘要
- en: In this chapter, we covered how to build your first server. For our first server,
    we used the STDIO transport to create a server meant to be run on our machine.
    We also looked into various ways of testing out the server features using a tool
    called the inspector. The inspector tool has two different modalities, CLI mode
    and visual mode. The former mode is used for CI/CD scenarios, and the latter for
    you as a developer quickly trying out features.
  id: totrans-175
  prefs: []
  type: TYPE_NORMAL
  zh: 在本章中，我们介绍了如何构建您的第一个服务器。对于我们的第一个服务器，我们使用了STDIO传输来创建一个旨在在您的机器上运行的服务器。我们还探讨了使用一个名为检查器的工具测试服务器功能的各种方法。检查器工具有两种不同的模式，CLI模式和视觉模式。前者模式用于CI/CD场景，后者用于您作为开发者快速尝试功能。
- en: In our upcoming chapter, we’ll describe the SSE transport, which you can use
    if you want the server to be consumed via a URL address.
  id: totrans-176
  prefs: []
  type: TYPE_NORMAL
  zh: 在我们即将到来的章节中，我们将描述SSE传输，如果您希望服务器通过URL地址被消费，可以使用它。
- en: Assignment – an e-commerce STDIO server
  id: totrans-177
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 作业 - 电子商务STDIO服务器
- en: For this MCP server, we will add capabilities that can be used in the context
    of an e-commerce application. Therefore, the server will need the following features.
  id: totrans-178
  prefs: []
  type: TYPE_NORMAL
  zh: 对于这个MCP服务器，我们将添加可以在电子商务应用程序上下文中使用的功能。因此，服务器需要以下功能。
- en: 'It will need these tools:'
  id: totrans-179
  prefs: []
  type: TYPE_NORMAL
  zh: 它将需要以下工具：
- en: '`get-orders`: This tool will return a list of orders. The optional input is
    a customer ID, and the output is a list of orders. Each order should contain the
    following fields: ID, customer ID, quantity, total price, and status.'
  id: totrans-180
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`get-orders`: 此工具将返回订单列表。可选输入是客户ID，输出是订单列表。每个订单应包含以下字段：ID、客户ID、数量、总价和状态。'
- en: '`get-order`: This tool will return a specific order. The input is the order
    ID, and the output is an order. The order should contain the following fields:
    ID, customer ID, quantity, total price, and status.'
  id: totrans-181
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`get-order`: 此工具将返回特定订单。输入是订单ID，输出是订单。订单应包含以下字段：ID、客户ID、数量、总价和状态。'
- en: '`place-order`: This tool will place an order. The input is the customer ID
    and the cart ID.'
  id: totrans-182
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`place-order`: 此工具将下订单。输入是客户ID和购物车ID。'
- en: '`get-cart`: This tool will return a cart.'
  id: totrans-183
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`get-cart`: 此工具将返回一个购物车。'
- en: '`get-cart-items`: This tool will return a list of items in the cart. The input
    is the cart ID, and the output is a list of items. Each item should contain the
    following fields: ID, name, description, price, quantity, and product ID.'
  id: totrans-184
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`get-cart-items`: 此工具将返回购物车中的项目列表。输入是购物车ID，输出是项目列表。每个项目应包含以下字段：ID、名称、描述、价格、数量和产品ID。'
- en: '`add-to-cart`: This tool will add an item to the cart. The input is the cart
    ID, product ID, and quantity. The output is a success message. If the cart ID
    is not provided, the tool should create a new cart and add the item to the new
    cart. The output should be a success message and the cart ID.'
  id: totrans-185
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`add-to-cart`: 此工具将项目添加到购物车。输入是购物车ID、产品ID和数量。输出是成功消息。如果未提供购物车ID，则工具应创建一个新的购物车并将项目添加到新购物车。输出应包含成功消息和购物车ID。'
- en: '`products`: This tool will return a list of products. The optional input is
    a category, and the output is a list of products. The product should contain the
    following fields: ID, name, description, price, and category. The output should
    be in JSON format.'
  id: totrans-186
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`products`: 此工具将返回产品列表。可选输入是类别，输出是产品列表。产品应包含以下字段：ID、名称、描述、价格和类别。输出应以JSON格式。'
- en: '`product`: This tool should return a specific product. The input is the product
    ID, and the output is a product. The product should contain the following fields:
    name, description, price, category, and ID. The output should be in JSON format.'
  id: totrans-187
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`product`: 此工具应返回特定产品。输入是产品ID，输出是产品。产品应包含以下字段：名称、描述、价格、类别和ID。输出应以JSON格式。'
- en: '`categories`: This tool will return a list of categories. The output should
    be in JSON format. The categories should contain the following fields: ID, name,
    and description. The output should be in JSON format.'
  id: totrans-188
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`categories`: 此工具将返回一个类别列表。输出应以JSON格式。类别应包含以下字段：ID、名称和描述。输出应以JSON格式。'
- en: '`get-customers`: This tool will return a list of customers. The output should
    be in JSON format. It should contain the following fields: ID, name, and email.'
  id: totrans-189
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`get-customers`: 此工具将返回客户列表。输出应为 JSON 格式。它应包含以下字段：ID、姓名和电子邮件。'
- en: 'It will need this resource:'
  id: totrans-190
  prefs: []
  type: TYPE_NORMAL
  zh: 它将需要此资源：
- en: '`product_catalog`: This resource will return a list of products in the catalog.
    The optional input is a category, and the output is a list of products. Each product
    should contain the following fields: name, description, and category. The idea
    is that the resource will return a list of products in the catalog, in case potential
    customers want to see them.'
  id: totrans-191
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`product_catalog`: 此资源将返回目录中的产品列表。可选输入是类别，输出是产品列表。每个产品应包含以下字段：名称、描述和类别。目的是，如果潜在客户想查看它们，资源将返回目录中的产品列表。'
- en: As you can see, this will support a simple e-commerce application and a customer
    trying to either put items in a cart or place an order.
  id: totrans-192
  prefs: []
  type: TYPE_NORMAL
  zh: 如你所见，这将支持一个简单的电子商务应用和一个试图将商品放入购物车或下订单的客户。
- en: You can keep state within memory data structures.
  id: totrans-193
  prefs: []
  type: TYPE_NORMAL
  zh: 你可以在内存数据结构中保持状态。
- en: Solution
  id: totrans-194
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 解决方案
- en: You can find the solution at [https://github.com/PacktPublishing/Learn-Model-Context-Protocol-with-Python/blob/main/Chapter03/solutions/README.md](https://github.com/PacktPublishing/Learn-Model-Context-Protocol-with-Python/blob/main/Chapter03/solutions/README.md).
  id: totrans-195
  prefs: []
  type: TYPE_NORMAL
  zh: 你可以在 [https://github.com/PacktPublishing/Learn-Model-Context-Protocol-with-Python/blob/main/Chapter03/solutions/README.md](https://github.com/PacktPublishing/Learn-Model-Context-Protocol-with-Python/blob/main/Chapter03/solutions/README.md)
    找到解决方案。
- en: Quiz
  id: totrans-196
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 问答
- en: What of the following is something a server can expose?
  id: totrans-197
  prefs: []
  type: TYPE_NORMAL
  zh: 以下哪项是服务器可以公开的内容？
- en: 'A: Tools, prompts, and services'
  id: totrans-198
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 'A: 工具、提示和服务'
- en: 'B: Tools and prompts'
  id: totrans-199
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 'B: 工具和提示'
- en: 'C: Prompts, tools, and resources'
  id: totrans-200
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 'C: 提示、工具和资源'
- en: You can find the solution at [https://github.com/PacktPublishing/Learn-Model-Context-Protocol-with-Python/blob/main/Chapter03/solutions/solution-quiz.md](https://github.com/PacktPublishing/Learn-Model-Context-Protocol-with-Python/blob/main/Chapter03/solutions/solution-quiz.md).
  id: totrans-201
  prefs: []
  type: TYPE_NORMAL
  zh: 你也可以在 [https://github.com/PacktPublishing/Learn-Model-Context-Protocol-with-Python/blob/main/Chapter03/solutions/solution-quiz.md](https://github.com/PacktPublishing/Learn-Model-Context-Protocol-with-Python/blob/main/Chapter03/solutions/solution-quiz.md)
    找到解决方案。
- en: References
  id: totrans-202
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 参考资料
- en: "**Inspector tool**: [https://github.com/modelcontextprotocol/inspector](https://github.com\uFEFF\
    /modelcontextprotocol/inspector)"
  id: totrans-203
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**检查工具**: [https://github.com/modelcontextprotocol/inspector](https://github.com/modelcontextprotocol/inspector)'
- en: "**Model Context Protocol**: [https://github.com/modelcontextprotocol/](https://g\uFEFF\
    ithub.com/modelcontextprotocol/)"
  id: totrans-204
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**模型上下文协议**: [https://github.com/modelcontextprotocol/](https://github.com/modelcontextprotocol/)'
- en: '**Python SDK**: [https://github.com/modelcontextprotocol/python-sdk](https://github.com/modelcontextprotocol/python-sdk)'
  id: totrans-205
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**Python SDK**: [https://github.com/modelcontextprotocol/python-sdk](https://github.com/modelcontextprotocol/python-sdk)'
- en: '|'
  id: totrans-206
  prefs: []
  type: TYPE_NORMAL
  zh: '|'
- en: Unlock this book’s exclusive benefits now
  id: totrans-207
  prefs:
  - PREF_H4
  type: TYPE_NORMAL
  zh: 现在解锁本书的独家优惠
- en: Scan this QR code or go to [https://packtpub.com/unlock](https://packtpub.com/unlock),
    then search this book by name. | ![](img/Unlock-01.png)![](img/Unlock1.png) |
  id: totrans-208
  prefs: []
  type: TYPE_NORMAL
  zh: 扫描此二维码或访问 [https://packtpub.com/unlock](https://packtpub.com/unlock)，然后通过名称搜索此书。
    | ![](img/Unlock-01.png)![](img/Unlock1.png) |
- en: '| **Note***: Keep your purchase invoice ready before you start.* |'
  id: totrans-209
  prefs: []
  type: TYPE_TB
  zh: '| **注意**：在开始之前，请准备好您的购买发票。* |'
