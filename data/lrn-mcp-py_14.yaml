- en: 'Appendix: Building for the Web with Modern Python'
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 附录：使用现代Python构建Web
- en: This is for those of you who want to learn about using Python and its modern
    constructs. You might have just switched to Python due to everyone using it because
    of AI, or maybe you’re fairly new to programming. Regardless, a hearty welcome;
    this appendix is for you. The goal with it is to make sure that when you read
    the other chapters, you understand all the constructs used and can use them to
    your benefit.
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: 这是为了那些想要了解如何使用Python及其现代构造的你们。你们可能是因为AI的普及而刚刚转向Python，或者可能你们对编程还比较新手。无论如何，热烈的欢迎；这个附录是为你们准备的。它的目标是确保当你们阅读其他章节时，能够理解所有使用的构造，并能够从中受益。
- en: Type hints and data modeling
  id: totrans-2
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 类型提示和数据建模
- en: Even though Python doesn’t enforce types, it definitely benefits from them.
    Type hints can make your code more readable and help catch errors early.
  id: totrans-3
  prefs: []
  type: TYPE_NORMAL
  zh: 尽管Python不强制类型，但它确实从类型中受益。类型提示可以使代码更易于阅读，并有助于早期捕获错误。
- en: 'Here’s some code without a type hint on adding a product to a basket:'
  id: totrans-4
  prefs: []
  type: TYPE_NORMAL
  zh: 下面是一些没有类型提示的将产品添加到购物篮的代码：
- en: '[PRE0]'
  id: totrans-5
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: While this code works, it lacks clarity on what type of product is being added
    to the basket. There’s also a risk that we make a mistake when accessing `product`
    attributes. You should therefore consider using type hints.
  id: totrans-6
  prefs: []
  type: TYPE_NORMAL
  zh: 虽然这段代码可以工作，但它缺乏关于添加到购物篮中的产品类型的清晰性。还有在访问`product`属性时出错的风险。因此，你应该考虑使用类型提示。
- en: Improved code with type hints
  id: totrans-7
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 带有类型提示的改进代码
- en: Type hints greatly help your IDE tooling, making it easier to catch errors and
    understand code. They also help with readability. Type hints such as `str`, `int`,
    and so on just work without the need to add a library. There’s also the `typing`
    library, which brings in additional types such as `List` and `Optional`, for example.
  id: totrans-8
  prefs: []
  type: TYPE_NORMAL
  zh: 类型提示极大地帮助了IDE工具，使其更容易捕获错误并理解代码。它们也有助于提高可读性。例如，`str`、`int`等类型提示无需添加库即可正常工作。还有`typing`库，它引入了额外的类型，例如`List`和`Optional`。
- en: 'The code shown previously can be made a lot more readable using type hints:'
  id: totrans-9
  prefs: []
  type: TYPE_NORMAL
  zh: 使用类型提示可以使之前显示的代码变得更加易于阅读：
- en: '[PRE1]'
  id: totrans-10
  prefs: []
  type: TYPE_PRE
  zh: '[PRE1]'
- en: 'This is a lot better as `product` is now clearly a dictionary and `basket`
    is a list of dictionaries. We can improve this even more by making `product` into
    a class, like so:'
  id: totrans-11
  prefs: []
  type: TYPE_NORMAL
  zh: 这已经好多了，因为`product`现在明显是一个字典，而`basket`是一个字典列表。我们可以通过将`product`转换为类来进一步改进这一点，如下所示：
- en: '[PRE2]'
  id: totrans-12
  prefs: []
  type: TYPE_PRE
  zh: '[PRE2]'
- en: What’s even better than this? The answer is a library such as **Pydantic**.
  id: totrans-13
  prefs: []
  type: TYPE_NORMAL
  zh: 什么比这更好？答案是像**Pydantic**这样的库。
- en: Adding Pydantic
  id: totrans-14
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 添加Pydantic
- en: 'So, what problems do we have that warrant using a validation library? Here
    are a few:'
  id: totrans-15
  prefs: []
  type: TYPE_NORMAL
  zh: 那么，我们有哪些问题需要使用验证库呢？以下是一些例子：
- en: '**Ensuring data integrity** to ensure that the data being processed is accurate
    and reliable'
  id: totrans-16
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**确保数据完整性**，以确保正在处理的数据是准确和可靠的'
- en: '**Reducing boilerplate code**, as it can automatically generate validation
    logic'
  id: totrans-17
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**减少样板代码**，因为它可以自动生成验证逻辑'
- en: '**Providing clear error messages**, making it easier to debug issues'
  id: totrans-18
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**提供清晰的错误消息**，使调试问题更容易'
- en: '**Simplifying complex data validation logic**, allowing for more maintainable
    code'
  id: totrans-19
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**简化复杂的数据验证逻辑**，使代码更易于维护'
- en: Without Pydantic
  id: totrans-20
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 没有使用Pydantic
- en: 'Imagine for a second that you have data coming in from a web request; the data
    is in the form of a dictionary, but needs to be a specific type, so we need to
    convert it to fix it. Here’s our situation in Python code:'
  id: totrans-21
  prefs: []
  type: TYPE_NORMAL
  zh: 想象一下，如果你从网络请求中接收数据；数据以字典的形式存在，但需要特定类型，因此我们需要将其转换为修正它。以下是我们用Python代码描述的情况：
- en: '[PRE3]'
  id: totrans-22
  prefs: []
  type: TYPE_PRE
  zh: '[PRE3]'
- en: Here, we have a situation where the data is in a dictionary shape, `product_dictionary`,
    but we need to convert it to a specific type, `Product`. We need to create a `from_dict`
    function to handle this conversion. This is where Pydantic shines, as it can help
    us validate and parse this data easily.
  id: totrans-23
  prefs: []
  type: TYPE_NORMAL
  zh: 在这里，我们有一个数据以字典形式存在的情况，即`product_dictionary`，但我们需要将其转换为特定类型`Product`。我们需要创建一个`from_dict`函数来处理这种转换。这正是Pydantic大放异彩的地方，因为它可以帮助我们轻松验证和解析这些数据。
- en: With Pydantic
  id: totrans-24
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 使用Pydantic
- en: 'Enter Pydantic, and let’s see how we can leverage it for our use case. By inheriting
    from `BaseModel`, we can create a model that automatically validates and parses
    our input data. See the following example where we define a `Product` model:'
  id: totrans-25
  prefs: []
  type: TYPE_NORMAL
  zh: 介绍Pydantic，让我们看看我们如何利用它来满足我们的用例。通过从`BaseModel`继承，我们可以创建一个模型，该模型会自动验证和解析我们的输入数据。请看以下示例，其中我们定义了一个`Product`模型：
- en: '[PRE4]'
  id: totrans-26
  prefs: []
  type: TYPE_PRE
  zh: '[PRE4]'
- en: With the preceding code, we have eliminated the need for a manual conversion
    function. Pydantic takes care of validating and parsing the input data, ensuring
    that it conforms to the expected structure. This not only simplifies our code
    but also makes it more robust and easier to maintain.
  id: totrans-27
  prefs: []
  type: TYPE_NORMAL
  zh: 在前面的代码中，我们消除了手动转换函数的需求。Pydantic 负责验证和解析输入数据，确保其符合预期的结构。这不仅简化了我们的代码，还使其更加健壮且易于维护。
- en: 'But there’s a problem here: if we feed the dictionary with incorrect data types
    or missing fields, Pydantic will raise a validation error. This code will crash,
    in fact, so let’s see how we can ensure that we capture any validation errors.'
  id: totrans-28
  prefs: []
  type: TYPE_NORMAL
  zh: 但这里有一个问题：如果我们向字典中提供错误的数据类型或缺失的字段，Pydantic 将引发验证错误。实际上，这段代码会导致崩溃，所以让我们看看我们如何确保捕获任何验证错误。
- en: Converting with confidence
  id: totrans-29
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 有信心地进行转换
- en: 'If the dictionary doesn’t conform to the expected structure, Pydantic will
    raise a validation error, making it clear what went wrong. Let’s make sure we
    capture any validation errors:'
  id: totrans-30
  prefs: []
  type: TYPE_NORMAL
  zh: 如果字典不符合预期的结构，Pydantic 将引发验证错误，清楚地表明出了什么问题。让我们确保我们捕获任何验证错误：
- en: '[PRE5]'
  id: totrans-31
  prefs: []
  type: TYPE_PRE
  zh: '[PRE5]'
- en: With this code, we wrap our call to Pydantic in a `try`-`except` block to handle
    any validation errors gracefully. Pydantic does more than just take strings, numbers,
    and Booleans; it can also handle more complex data types such as lists and dictionaries.
  id: totrans-32
  prefs: []
  type: TYPE_NORMAL
  zh: 使用这段代码，我们用 `try`-`except` 块包装对 Pydantic 的调用，以优雅地处理任何验证错误。Pydantic 不仅处理字符串、数字和布尔值；它还可以处理更复杂的数据类型，如列表和字典。
- en: More advanced objects
  id: totrans-33
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 更高级的对象
- en: 'Let’s look at a slightly more complex example featuring a professor and their
    office hours availability:'
  id: totrans-34
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们来看一个稍微复杂一些的例子，其中包含一位教授及其办公时间可用性：
- en: '[PRE6]'
  id: totrans-35
  prefs: []
  type: TYPE_PRE
  zh: '[PRE6]'
- en: In this code, we define a `Professor` model that includes an ID, name, and a
    list of office hours. Each office hour is represented by an `OfficeHour` model,
    which includes the day of the week and the start and end times. This structure
    allows us to easily validate and work with complex data types using Pydantic.
  id: totrans-36
  prefs: []
  type: TYPE_NORMAL
  zh: 在此代码中，我们定义了一个包含 ID、姓名和办公时间列表的 `Professor` 模型。每个办公时间由一个 `OfficeHour` 模型表示，该模型包括星期几以及开始和结束时间。这种结构使我们能够轻松地使用
    Pydantic 验证和操作复杂的数据类型。
- en: Serialization
  id: totrans-37
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 序列化
- en: 'Sometimes we have a situation where we need to go from a Pydantic model instance
    back to a dictionary. This can be useful for serialization or when we need to
    interact with APIs that expect data in a specific format. For this, we can use
    `model_dump`, a method that exists on each model. Here’s how to use it:'
  id: totrans-38
  prefs: []
  type: TYPE_NORMAL
  zh: 有时我们会有这样的情况，需要从 Pydantic 模型实例回到字典。这在序列化或我们需要与期望特定格式的 API 交互时很有用。为此，我们可以使用存在于每个模型上的
    `model_dump` 方法。以下是使用方法：
- en: '[PRE7]'
  id: totrans-39
  prefs: []
  type: TYPE_PRE
  zh: '[PRE7]'
- en: 'You can even decide what fields are being included using `model_dump(include=...)`
    and `model_dump(exclude=...)`:'
  id: totrans-40
  prefs: []
  type: TYPE_NORMAL
  zh: 您甚至可以使用 `model_dump(include=...)` 和 `model_dump(exclude=...)` 决定包含哪些字段：
- en: '[PRE8]'
  id: totrans-41
  prefs: []
  type: TYPE_PRE
  zh: '[PRE8]'
- en: Pydantic is used heavily in MCP internally in the SDK, and you’re encouraged
    to use it for input and output validation.
  id: totrans-42
  prefs: []
  type: TYPE_NORMAL
  zh: Pydantic 在 MCP 内部 SDK 中被大量使用，并鼓励您在输入和输出验证中使用它。
- en: "You can even write our own validators, but I’ll leave that as an exercise for\
    \ you. See the official docs at [https://docs.pydantic.dev/latest/concepts/validators/](https://docs.pydantic.dev/latest/concepts/\uFEFF\
    validators/)."
  id: totrans-43
  prefs: []
  type: TYPE_NORMAL
  zh: 您甚至可以编写自己的验证器，但我会把这留作您的练习。请参阅官方文档[https://docs.pydantic.dev/latest/concepts/validators/](https://docs.pydantic.dev/latest/concepts/validators/)。
- en: async and await
  id: totrans-44
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: async 和 await
- en: Validating code is important, but another important aspect we also need to understand
    is asynchronous programming. Asynchronous programming allows us to write code
    that can perform multiple tasks at once without blocking the main thread. This
    is particularly useful in scenarios where we need to handle I/O-bound operations,
    such as making API calls or reading from a database.
  id: totrans-45
  prefs: []
  type: TYPE_NORMAL
  zh: 验证代码很重要，但我们还需要理解另一个重要方面，那就是异步编程。异步编程允许我们编写能够同时执行多个任务而不阻塞主线程的代码。这在需要处理 I/O 密集型操作的场景中特别有用，例如进行
    API 调用或从数据库中读取。
- en: 'Let’s talk about **async/await**, which is a syntax for writing asynchronous
    code in Python. You should use `async`/`await` when you want to write non-blocking
    code that can handle many tasks at once. If your code is blocking, then the end
    user experience suffers, as they have to wait for each task to complete before
    moving on. Imagine the problem multiplying on a web server with many users. Okay,
    so what do we need to know? First, let’s start with the concepts:'
  id: totrans-46
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们谈谈 **async/await**，这是在 Python 中编写异步代码的语法。当你想要编写非阻塞代码，能够同时处理多个任务时，你应该使用 `async`/`await`。如果你的代码是阻塞的，那么最终用户的体验会受到影响，因为他们必须等待每个任务完成才能继续。想象一下在拥有许多用户的
    Web 服务器上这个问题是如何成倍的。好吧，那么我们需要知道什么？首先，让我们从概念开始：
- en: '`coroutine`: A so-called coroutine is created when you mark up a function with
    `async def`. That means this function can be paused and resumed, allowing other
    tasks to run in the meantime. Let’s see this pause and resume behavior in the
    following code:'
  id: totrans-47
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`协程`：当你用 `async def` 标记一个函数时，就创建了一个所谓的协程。这意味着这个函数可以被暂停和恢复，允许在此期间运行其他任务。让我们在下面的代码中看看这个暂停和恢复的行为：'
- en: '[PRE9]'
  id: totrans-48
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE9]'
- en: In this code, we define `fetch_data`, a function marked with the `async` keyword.
    The function itself calls `await asyncio.sleep(1)`, meaning we want the code to
    *stop* here for one second. Then, we *resume* and return a dictionary. Is this
    non-blocking? Yes, because when `asyncio.sleep` is running, other work can take
    place.
  id: totrans-49
  prefs: []
  type: TYPE_NORMAL
  zh: 在此代码中，我们定义了一个带有 `async` 关键字的函数 `fetch_data`。该函数本身调用 `await asyncio.sleep(1)`，这意味着我们希望代码在这里停止一秒钟。然后，我们恢复并返回一个字典。这是非阻塞的吗？是的，因为当
    `asyncio.sleep` 运行时，其他工作可以继续进行。
- en: '**Event loop**: The event loop is what runs the coroutines and manages their
    execution. To interact with the event loop and run your `async` code, call `asyncio.run(fetch_data())`:'
  id: totrans-50
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '**事件循环**：事件循环是运行协程并管理其执行的部分。要与事件循环交互并运行你的 `async` 代码，请调用 `asyncio.run(fetch_data())`：'
- en: '[PRE10]'
  id: totrans-51
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE10]'
- en: 'Another way to talk to the event loop is by calling `asyncio.get_running_loop()`.
    This will give you the current event loop instance:'
  id: totrans-52
  prefs: []
  type: TYPE_NORMAL
  zh: 另一种与事件循环交互的方式是通过调用 `asyncio.get_running_loop()`。这将给你当前的事件循环实例：
- en: '`await`: You’ve seen this in use already, but any function marked with `async`
    should use `await` when it’s called.'
  id: totrans-53
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`await`：你已经看到它的使用了，但任何标记为 `async` 的函数在调用时都应该使用 `await`。'
- en: '`asyncio`: We showed this already by calling its `run` method. It is a library
    in Python that provides support for asynchronous programming. It allows you to
    write concurrent code using the `async`/`await` syntax. You’ll often use `asyncio`
    when working with web frameworks such as FastAPI.'
  id: totrans-54
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`asyncio`：我们已经在调用它的 `run` 方法时展示了它。它是一个在 Python 中提供异步编程支持的库。它允许你使用 `async`/`await`
    语法编写并发代码。当你在与 FastAPI 等网络框架一起工作时，你经常会使用 `asyncio`。'
- en: 'Let’s see a web application example using FastAPI:'
  id: totrans-55
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们看看使用 FastAPI 的一个 Web 应用程序示例：
- en: '[PRE11]'
  id: totrans-56
  prefs: []
  type: TYPE_PRE
  zh: '[PRE11]'
- en: '`asyncio.gather`: `asyncio` provides another useful method called `gather`,
    which allows you to run multiple coroutines concurrently and wait for all of them
    to finish. It’s often more convenient than `wait` when you need the results of
    all tasks. Here’s how to use it:'
  id: totrans-57
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`asyncio.gather`：`asyncio` 提供了另一个有用的方法，名为 `gather`，它允许你并发运行多个协程并等待它们全部完成。当你需要所有任务的结果时，它通常比
    `wait` 更方便。以下是使用它的方法：'
- en: '[PRE12]'
  id: totrans-58
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE12]'
- en: Here, each call to `fetch_data` is passed as an argument to `gather`. While
    `gather` might be more convenient, it does not provide as much control over individual
    tasks as `wait` does. Speaking of `wait`, let’s see how it works.
  id: totrans-59
  prefs: []
  type: TYPE_NORMAL
  zh: 在这里，每次调用 `fetch_data` 都将作为参数传递给 `gather`。虽然 `gather` 可能更方便，但它并不像 `wait` 那样提供对单个任务的很多控制。说到
    `wait`，让我们看看它是如何工作的。
- en: '`asyncio.wait`: `asyncio` has useful methods, and one such method is `wait`,
    which allows you to wait for multiple tasks to complete. This is particularly
    useful when you want to run several tasks concurrently and wait for all of them
    to finish. Here’s how to use it:'
  id: totrans-60
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`asyncio.wait`：`asyncio` 提供了一些有用的方法，其中之一就是 `wait` 方法，它允许你等待多个任务完成。当你想要并发运行多个任务并等待它们全部完成时，这特别有用。以下是使用它的方法：'
- en: '[PRE13]'
  id: totrans-61
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE13]'
- en: Here, the code creates three search tasks, each with a different delay and workload.
    The tasks are started concurrently using `asyncio.create_task`. The `asyncio.as_completed`
    function is used to process the results as they finish. If a task finds the target
    value, it sets the `stop` event, which cancels the other tasks. Finally, all tasks
    are awaited to ensure proper cleanup.
  id: totrans-62
  prefs: []
  type: TYPE_NORMAL
  zh: 在这里，代码创建了三个搜索任务，每个任务都有不同的延迟和工作负载。这些任务通过 `asyncio.create_task` 并发启动。使用 `asyncio.as_completed`
    函数来处理完成的结果。如果一个任务找到了目标值，它将设置 `stop` 事件，这将取消其他任务。最后，所有任务都被等待以确保适当的清理。
- en: 'As you can see, there’s a lot of flexibility in how you can manage concurrent
    tasks in Python using `asyncio`. Let’s just mention one more thing: so far, you’ve
    seen how your methods can be made `async`, and even your web frameworks. There’s
    also a specific library for making web requests `async`, called `httpx`.'
  id: totrans-63
  prefs: []
  type: TYPE_NORMAL
  zh: 正如您所看到的，在Python中使用`asyncio`管理并发任务的方式有很多灵活性。让我们再提一点：到目前为止，您已经看到了如何使您的函数`async`化，甚至您的Web框架。还有一个专门用于使Web请求`async`化的库，称为`httpx`。
- en: '`httpx` is a fully featured HTTP client for Python 3 that provides `async`
    capabilities. It allows you to make HTTP requests in an asynchronous manner, making
    it a great choice for I/O-bound tasks. Here’s a simple example of how to use `httpx`
    with `async`:'
  id: totrans-64
  prefs: []
  type: TYPE_NORMAL
  zh: '`httpx`是一个功能齐全的Python 3 HTTP客户端，它提供了`async`能力。它允许您以异步方式发出HTTP请求，这使得它非常适合I/O密集型任务。以下是一个如何使用`httpx`和`async`的简单示例：'
- en: '[PRE14]'
  id: totrans-65
  prefs: []
  type: TYPE_PRE
  zh: '[PRE14]'
- en: Great, now that we have a better grasp on `async`, let’s see how we can use
    it in a web context.
  id: totrans-66
  prefs: []
  type: TYPE_NORMAL
  zh: 太好了，现在我们对`async`有了更好的理解，让我们看看如何在Web环境中使用它。
- en: 'Uvicorn: the ASGI server'
  id: totrans-67
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: Uvicorn：ASGI服务器
- en: First, let’s see where we came from, namely, WSGI. **WSGI** stands for **Web
    Server Gateway Interface**, a standard interface between web servers and Python
    web applications. It has been the de facto standard for Python web applications
    for many years. However, the **Asynchronous Server Gateway Interface** (**ASGI**)
    was introduced to address the needs of modern web applications, particularly those
    requiring asynchronous capabilities. So, with ASGI, we can handle asynchronous
    requests more efficiently in a way we couldn’t with WSGI.
  id: totrans-68
  prefs: []
  type: TYPE_NORMAL
  zh: 首先，让我们看看我们是从哪里来的，即WSGI。**WSGI**代表**Web Server Gateway Interface**，它是Web服务器和Python
    Web应用之间的一个标准接口。多年来，它一直是Python Web应用的事实标准。然而，为了满足现代Web应用的需求，特别是那些需要异步能力的应用，引入了**异步服务器网关接口**（**ASGI**）。因此，使用ASGI，我们可以更有效地处理异步请求，这是WSGI所不能做到的。
- en: 'Uvicorn is a lightning-fast ASGI server implementation, using `uvloop` and
    `httptools`. It’s perfect for serving your FastAPI applications and can handle
    both HTTP and WebSocket protocols. `uvloop` is a fast implementation of the event
    loop, which is the core of asynchronous programming in Python. `httptools` is
    a library for parsing HTTP requests and responses. Uvicorn works seamlessly with
    FastAPI and can, for example, start a server, enabling your application to handle
    requests concurrently. Here’s a web server example:'
  id: totrans-69
  prefs: []
  type: TYPE_NORMAL
  zh: Uvicorn是一个快速的ASGI服务器实现，使用`uvloop`和`httptools`。它非常适合提供FastAPI应用服务，可以处理HTTP和WebSocket协议。`uvloop`是事件循环的快速实现，这是Python异步编程的核心。`httptools`是一个用于解析HTTP请求和响应的库。Uvicorn与FastAPI无缝协作，例如，它可以启动服务器，使您的应用能够并发处理请求。以下是一个Web服务器示例：
- en: '[PRE15]'
  id: totrans-70
  prefs: []
  type: TYPE_PRE
  zh: '[PRE15]'
- en: 'You can run this FastAPI application with Uvicorn using the following command:'
  id: totrans-71
  prefs: []
  type: TYPE_NORMAL
  zh: 您可以使用以下命令使用Uvicorn运行此FastAPI应用：
- en: '[PRE16]'
  id: totrans-72
  prefs: []
  type: TYPE_PRE
  zh: '[PRE16]'
- en: What the preceding code does is to run a file called `main.py` and look for
    an instance of the FastAPI application called `app`. It will then start the server
    and enable hot-reloading, so any changes you make to your code will be automatically
    applied without needing to restart the server.
  id: totrans-73
  prefs: []
  type: TYPE_NORMAL
  zh: 前面的代码所做的是运行一个名为`main.py`的文件，并查找名为`app`的FastAPI应用实例。然后它将启动服务器并启用热重载，因此您对代码所做的任何更改都将自动应用，无需重新启动服务器。
- en: 'You can also specify additional options such as the host and port:'
  id: totrans-74
  prefs: []
  type: TYPE_NORMAL
  zh: 您还可以指定其他选项，例如主机和端口：
- en: '[PRE17]'
  id: totrans-75
  prefs: []
  type: TYPE_PRE
  zh: '[PRE17]'
- en: This is just the beginning of using Uvicorn, but it’s a good start to be able
    to create MCP servers using transports such as SSE or Streamable HTTP.
  id: totrans-76
  prefs: []
  type: TYPE_NORMAL
  zh: 这只是使用Uvicorn的开始，但能够使用SSE或Streamable HTTP等传输创建MCP服务器是一个很好的起点。
- en: Context managers
  id: totrans-77
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 上下文管理器
- en: 'Another thing you will see a lot when working with the MCP SDK is the use of
    context managers, so what are those? A context manager is a Python object that
    defines the runtime context to be established when executing a `with` statement.
    The most common use case is resource management, where you want to ensure that
    resources are properly acquired and released. For example, it’s a good idea to
    use it if you need to set up a resource such as a database connection or some
    other type of resource. Here’s a simple example:'
  id: totrans-78
  prefs: []
  type: TYPE_NORMAL
  zh: 在使用MCP SDK工作时，您还会经常看到上下文管理器的使用，那么这些是什么？上下文管理器是一个Python对象，它定义了执行`with`语句时要建立的运行时上下文。最常见的用例是资源管理，您希望确保资源被正确获取和释放。例如，如果您需要设置数据库连接或其他类型的资源，使用它是个好主意。以下是一个简单示例：
- en: '[PRE18]'
  id: totrans-79
  prefs: []
  type: TYPE_PRE
  zh: '[PRE18]'
- en: 'What makes us able to use the `with` keyword, you might ask. Well, context
    managers are able to use the `with` keyword because they implement two special
    methods: `__enter__` and `__exit__`. When the `with` statement is executed, the
    `__enter__` method is called, and when the block inside the `with` statement is
    exited, the `__exit__` method is called. This allows for setup and teardown actions
    to be performed automatically. Let’s see how the preceding class implements these
    methods:'
  id: totrans-80
  prefs: []
  type: TYPE_NORMAL
  zh: 您可能会问，是什么让我们能够使用`with`关键字。好吧，上下文管理器能够使用`with`关键字是因为它们实现了两个特殊方法：`__enter__`和`__exit__`。当执行`with`语句时，会调用`__enter__`方法，而当退出`with`语句内部的块时，会调用`__exit__`方法。这允许自动执行设置和清理操作。让我们看看前面的类是如何实现这些方法的：
- en: '[PRE19]'
  id: totrans-81
  prefs: []
  type: TYPE_PRE
  zh: '[PRE19]'
- en: The preceding code demonstrates the use of a context manager for managing database
    connections. You can see how the `__enter__` method establishes the connection
    and returns it, while the `__exit__` method ensures that the connection is closed
    when the block is exited, even if an exception occurs.
  id: totrans-82
  prefs: []
  type: TYPE_NORMAL
  zh: 上述代码演示了使用上下文管理器来管理数据库连接的使用方法。您可以看到`__enter__`方法是如何建立连接并返回它的，而`__exit__`方法确保在退出块时关闭连接，即使发生异常也是如此。
- en: 'There’s another version of `with`, namely, `async with`, which is used for
    asynchronous context managers. These are particularly useful when working with
    asynchronous code, such as when using `asyncio` or handling asynchronous I/O operations.
    The SDK uses this pattern in the client, for example, to make the initial server
    connection, like so:'
  id: totrans-83
  prefs: []
  type: TYPE_NORMAL
  zh: '`with`语句还有另一个版本，即`async with`，它用于异步上下文管理器。当处理异步代码时，这些特别有用，例如使用`asyncio`或处理异步I/O操作时。SDK在客户端使用这种模式，例如，用于建立初始服务器连接，如下所示：'
- en: '[PRE20]'
  id: totrans-84
  prefs: []
  type: TYPE_PRE
  zh: '[PRE20]'
- en: 'In this code, it now calls the `__aenter__` and `__aexit__` methods of the
    asynchronous context manager, which helps to create a server connection. If you
    keep looking at the SDK code for the client, you will see that the full initialization
    code looks like so:'
  id: totrans-85
  prefs: []
  type: TYPE_NORMAL
  zh: 在此代码中，现在它调用异步上下文管理器的`__aenter__`和`__aexit__`方法，这有助于创建服务器连接。如果您继续查看客户端的SDK代码，您将看到完整的初始化代码如下：
- en: '[PRE21]'
  id: totrans-86
  prefs: []
  type: TYPE_PRE
  zh: '[PRE21]'
- en: 'What’s going on here? Two asynchronous context managers are being used in tandem.
    The outer context manager (`stdio_client`) is responsible for managing the standard
    input/output streams for the server connection, while the inner context manager
    (`ClientSession`) is responsible for managing the lifecycle of the client session
    itself. From an implementation standpoint, here’s some dummy code:'
  id: totrans-87
  prefs: []
  type: TYPE_NORMAL
  zh: 这里发生了什么？这里正在联合使用两个异步上下文管理器。外部上下文管理器（`stdio_client`）负责管理服务器连接的标准输入/输出流，而内部上下文管理器（`ClientSession`）负责管理客户端会话的生命周期。从实现的角度来看，这里有一些示例代码：
- en: '[PRE22]'
  id: totrans-88
  prefs: []
  type: TYPE_PRE
  zh: '[PRE22]'
- en: Note how the `__aenter__` method in `stdio_client` is responsible for creating
    the client connection and returning the necessary streams, while the `__aenter__`
    method in `ClientSession` is responsible for initializing the session with those
    streams. So, now you hopefully understand why the code looks the way it does.
    That is, some needed initialization is happening when the context manager is created,
    and some clean up happens when it is exited. If we don’t use a pattern like this,
    it’s easy to forget the clean-up part, and we cause a resource leak.
  id: totrans-89
  prefs: []
  type: TYPE_NORMAL
  zh: 注意`stdio_client`中的`__aenter__`方法负责创建客户端连接并返回必要的流，而`ClientSession`中的`__aenter__`方法负责使用这些流初始化会话。所以，现在您可能已经理解为什么代码看起来是这个样子。也就是说，一些必要的初始化操作是在上下文管理器创建时发生的，而一些清理操作是在退出时发生的。如果我们不使用这种模式，很容易忘记清理部分，从而导致资源泄露。
- en: The contextmanager library
  id: totrans-90
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: '`contextmanager`库'
- en: 'The `contextmanager` library in Python provides a way to create context managers
    using generator functions. Instead of defining classes with the `__enter__` and
    `__exit__` methods, you can use the `@contextmanager` decorator to turn a generator
    function into a context manager. Let’s see an example of that:'
  id: totrans-91
  prefs: []
  type: TYPE_NORMAL
  zh: Python中的`contextmanager`库提供了一种使用生成器函数创建上下文管理器的方法。您不需要使用`__enter__`和`__exit__`方法来定义类，而是可以使用`@contextmanager`装饰器将生成器函数转换为上下文管理器。让我们看看一个例子：
- en: '[PRE23]'
  id: totrans-92
  prefs: []
  type: TYPE_PRE
  zh: '[PRE23]'
- en: You can see that we don’t need to write `__enter__` and `__exit__`, but can
    instead decorate a function with `@contextmanager` to achieve the same effect.
    This library is also used by the MCP SDK to set up context managers for various
    resources.
  id: totrans-93
  prefs: []
  type: TYPE_NORMAL
  zh: 您可以看到我们不需要编写`__enter__`和`__exit__`，而是可以用`@contextmanager`装饰器来装饰一个函数，以达到相同的效果。这个库也被MCP
    SDK用来为各种资源设置上下文管理器。
- en: 'uv: your development environment manager'
  id: totrans-94
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: uv：您的开发环境管理器
- en: 'Let’s cover our last topic in this appendix, namely, `uv`. It’s not something
    you must use, but you’re highly recommended to do so. Here’s why:'
  id: totrans-95
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们总结这个附录的最后一个主题，即 `uv`。这不是您必须使用的东西，但强烈建议您这样做。原因如下：
- en: It provides a consistent interface for managing your development environment.
    It means you can easily switch between different projects and their dependencies
    without worrying about conflicts.
  id: totrans-96
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 它提供了一个一致的界面来管理您的开发环境。这意味着您可以在不担心冲突的情况下轻松地在不同的项目和它们的依赖项之间切换。
- en: It streamlines the process of setting up and managing virtual environments,
    making it easier to work on multiple projects with different requirements. So,
    instead of typing `python -m venv env` and `source env/bin/activate`, you can
    simply use `uv init` to create and activate a virtual environment, which also
    initiates your project.
  id: totrans-97
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 它简化了设置和管理虚拟环境的过程，使您能够更容易地在具有不同要求的多个项目上工作。因此，您不必键入 `python -m venv env` 和 `source
    env/bin/activate`，只需简单地使用 `uv init` 来创建和激活虚拟环境，这也会启动您的项目。
- en: It simplifies the process of installing and managing dependencies, allowing
    you to easily add, remove, and update packages as needed. You’re used to typing
    `pip install <package>` to install a package, but with `uv`, you can use `uv add
    <package>` instead.
  id: totrans-98
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 它简化了安装和管理依赖项的过程，允许您轻松地按需添加、删除和更新包。您习惯于键入 `pip install <package>` 来安装一个包，但使用
    `uv`，您可以使用 `uv add <package>` 代替。
- en: Project management with `project.toml` is made easier, allowing you to define
    your project’s dependencies and settings in a single file instead of scattering
    them across multiple files.
  id: totrans-99
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 使用 `project.toml` 进行项目管理，使操作更简单，允许您在单个文件中定义您项目的依赖项和设置，而不是分散在多个文件中。
- en: You can even use `uv` to manage your Docker containers and images, making it
    easier to deploy your applications in a consistent environment.
  id: totrans-100
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 您甚至可以使用 `uv` 来管理您的 Docker 容器和镜像，使在一致的环境中部署您的应用程序变得更加容易。
- en: 'Here is what you can do:'
  id: totrans-101
  prefs: []
  type: TYPE_NORMAL
  zh: 您可以这样做：
- en: 'Install it with the following:'
  id: totrans-102
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
  zh: 使用以下命令安装：
- en: '[PRE24]'
  id: totrans-103
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE24]'
- en: 'Create a `uv` configuration file in your project root:'
  id: totrans-104
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
  zh: 在您的项目根目录中创建一个 `uv` 配置文件：
- en: '[PRE25]'
  id: totrans-105
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE25]'
- en: 'Add a dependency using the `uv add` command:'
  id: totrans-106
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
  zh: 使用 `uv add` 命令添加依赖项：
- en: '[PRE26]'
  id: totrans-107
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE26]'
- en: 'Run your app with the following:'
  id: totrans-108
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
  zh: 使用以下命令运行您的应用程序：
- en: '[PRE27]'
  id: totrans-109
  prefs:
  - PREF_IND
  type: TYPE_PRE
  zh: '[PRE27]'
- en: We’ve come to the end of this appendix. Hopefully, you feel more up to speed
    with modern Python and tools such as `uv`, `asyncio`, and Uvicorn.
  id: totrans-110
  prefs: []
  type: TYPE_NORMAL
  zh: 我们已经到达了这个附录的结尾。希望您对现代 Python 以及 `uv`、`asyncio` 和 Uvicorn 等工具有了更深入的了解。
- en: '|'
  id: totrans-111
  prefs: []
  type: TYPE_NORMAL
  zh: '|'
- en: Unlock this book’s exclusive benefits now
  id: totrans-112
  prefs:
  - PREF_H4
  type: TYPE_NORMAL
  zh: 现在解锁这本书的独家优惠
- en: Scan this QR code or go to [https://packtpub.com/unlock](https://packtpub.com/unlock),
    then search this book by name. | ![](img/Unlock-01.png)![](img/Unlock1.png) |
  id: totrans-113
  prefs: []
  type: TYPE_NORMAL
  zh: 扫描此二维码或访问 [https://packtpub.com/unlock](https://packtpub.com/unlock)，然后通过名称搜索此书。
    | ![解锁-01.png](img/Unlock-01.png)![](img/Unlock1.png) |
- en: '| **Note***: Keep your purchase invoice ready before you start.* |'
  id: totrans-114
  prefs: []
  type: TYPE_TB
  zh: '| **注意**：在开始之前，请准备好您的购买发票。* |'
- en: '![](img/New_Packt_Logo1.png)'
  id: totrans-115
  prefs: []
  type: TYPE_IMG
  zh: '![新 Packt 标志](img/New_Packt_Logo1.png)'
- en: '[www.packtub.com](https://www.packtub.com)'
  id: totrans-116
  prefs: []
  type: TYPE_NORMAL
  zh: '[www.packtub.com](https://www.packtub.com)'
- en: Subscribe to our online digital library for full access to over 7,000 books
    and videos, as well as industry leading tools to help you plan your personal development
    and advance your career. For more information, please visit our website.
  id: totrans-117
  prefs: []
  type: TYPE_NORMAL
  zh: 订阅我们的在线数字图书馆，全面访问超过 7,000 本书和视频，以及领先的工具，帮助您规划个人发展并推进您的职业生涯。更多信息，请访问我们的网站。
- en: Why subscribe?
  id: totrans-118
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 为什么订阅？
- en: Spend less time learning and more time coding with practical eBooks and Videos
    from over 4,000 industry professionals
  id: totrans-119
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 使用来自 4,000 多位行业专业人士的实用电子书和视频，减少学习时间，增加编码时间
- en: Improve your learning with Skill Plans built especially for you
  id: totrans-120
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 通过为您量身定制的技能计划提高您的学习效果
- en: Get a free eBook or video every month
  id: totrans-121
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 每月免费获得一本电子书或视频
- en: Fully searchable for easy access to vital information
  id: totrans-122
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 完全可搜索，便于轻松访问关键信息
- en: Copy and paste, print, and bookmark content
  id: totrans-123
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 复制粘贴、打印和收藏内容
- en: At [www.packtpub.com](https://www.packtpub.com), you can also read a collection
    of free technical articles, sign up for a range of free newsletters, and receive
    exclusive discounts and offers on Packt books and eBooks.
  id: totrans-124
  prefs: []
  type: TYPE_NORMAL
  zh: 在 [www.packtpub.com](https://www.packtpub.com)，您还可以阅读一系列免费的技术文章，订阅各种免费通讯，并享受
    Packt 书籍和电子书的独家折扣和优惠。
- en: Other Books You May Enjoy
  id: totrans-125
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 您可能还会喜欢的其他书籍
- en: 'If you enjoyed this book, you may be interested in these other books by Packt:'
  id: totrans-126
  prefs: []
  type: TYPE_NORMAL
  zh: 如果您喜欢这本书，您可能会对 Packt 的以下其他书籍感兴趣：
- en: '[![](img/9781803238944.jpg)](https://www.packtpub.com/en-us/product/nodejs-design-patterns-9781803238944)'
  id: totrans-127
  prefs: []
  type: TYPE_NORMAL
  zh: '![9781803238944.jpg](https://www.packtpub.com/en-us/product/nodejs-design-patterns-9781803238944)'
- en: '**Node.js Design Patterns, Fourth Edition**'
  id: totrans-128
  prefs: []
  type: TYPE_NORMAL
  zh: '**Node.js 设计模式，第四版**'
- en: Luciano Mammino and Mario Casciaro
  id: totrans-129
  prefs: []
  type: TYPE_NORMAL
  zh: 卢西亚诺·马米诺和马里奥·卡西亚罗
- en: 'ISBN: 978-1-80323-894-4'
  id: totrans-130
  prefs: []
  type: TYPE_NORMAL
  zh: 'ISBN: 978-1-80323-894-4'
- en: Understand Node.js basics and its async event-driven architecture
  id: totrans-131
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 理解Node.js基础知识及其异步事件驱动架构
- en: Write correct async code using callbacks, promises, and async/await
  id: totrans-132
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 使用回调、承诺和async/await编写正确的异步代码
- en: Harness Node.js streams to create data-driven processing pipelines
  id: totrans-133
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 利用 Node.js 流创建数据驱动的处理管道
- en: Implement trusted software design patterns for production-grade applications
  id: totrans-134
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 为生产级应用实现可信的软件设计模式
- en: Write testable code and automated tests (unit, integration, E2E)
  id: totrans-135
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 编写可测试的代码和自动化测试（单元测试、集成测试、端到端测试）
- en: 'Use advanced recipes: caching, batching, async init, offload CPU-bound work'
  id: totrans-136
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 使用高级食谱：缓存、批处理、异步初始化、卸载CPU密集型工作
- en: Build and scale microservices and distributed systems powered by Node.js
  id: totrans-137
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 使用Node.js构建和扩展微服务和分布式系统
- en: '[![](img/9781837028238.jpg)](https://www.packtpub.com/en-us/product/responsive-web-design-with-html5-and-css-9781837028238)'
  id: totrans-138
  prefs: []
  type: TYPE_NORMAL
  zh: '[![](img/9781837028238.jpg)](https://www.packtpub.com/en-us/product/responsive-web-design-with-html5-and-css-9781837028238)'
- en: '**Responsive Web Design with HTML5 and CSS, Fifth Edition**'
  id: totrans-139
  prefs: []
  type: TYPE_NORMAL
  zh: '**使用HTML5和CSS的响应式网页设计，第五版**'
- en: Ben Frain
  id: totrans-140
  prefs: []
  type: TYPE_NORMAL
  zh: 本·弗莱恩
- en: 'ISBN: 978-1-83702-823-8'
  id: totrans-141
  prefs: []
  type: TYPE_NORMAL
  zh: 'ISBN: 978-1-83702-823-8'
- en: Leverage color functions to mix colors and convert between color spaces
  id: totrans-142
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 利用颜色函数混合颜色并在颜色空间之间转换
- en: Use media and container queries to detect touch/mouse and color preference
  id: totrans-143
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 使用媒体查询和容器查询来检测触摸/鼠标和颜色偏好
- en: Leverage HTML semantics to author accessible markup
  id: totrans-144
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 利用HTML语义来编写可访问的标记
- en: Use SVGs to provide resolution-independent images and learn to efficiently display
    them
  id: totrans-145
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 使用SVG提供分辨率无关的图像，并学习高效地显示它们
- en: Create animations as items enter and leave the viewport using just CSS
  id: totrans-146
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 仅使用CSS创建动画，当项目进入和离开视口时
- en: Discover CSS custom properties and make use of new CSS functions
  id: totrans-147
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 发现CSS自定义属性并利用新的CSS函数
- en: Add validation and interface elements to HTML forms
  id: totrans-148
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 向HTML表单添加验证和界面元素
- en: Check whether the frontend code produced by AI tools is effective for your goals
  id: totrans-149
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 检查由AI工具生成的前端代码是否满足您的目标
- en: Packt is searching for authors like you
  id: totrans-150
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: Packt正在寻找像您这样的作者
- en: If you’re interested in becoming an author for Packt, please visit [authors.packtpub.com](https://authors.packtpub.com)
    and apply today. We have worked with thousands of developers and tech professionals,
    just like you, to help them share their insight with the global tech community.
    You can make a general application, apply for a specific hot topic that we are
    recruiting an author for, or submit your own idea.
  id: totrans-151
  prefs: []
  type: TYPE_NORMAL
  zh: 如果您有兴趣成为Packt的作者，请访问[authors.packtpub.com](https://authors.packtpub.com)并今天申请。我们已与成千上万的开发人员和科技专业人士合作，就像您一样，帮助他们与全球科技社区分享他们的见解。您可以提交一般申请，申请我们正在招募作者的特定热门话题，或提交您自己的想法。
- en: Share your thoughts
  id: totrans-152
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 分享您的想法
- en: Now you’ve finished Learn Model Context Protocol with Python, we’d love to hear
    your thoughts! If you purchased the book from Amazon, please [click here to go
    straight to the Amazon review page](Other_Book_You_May_Enjoy.xhtml) for this book
    and share your feedback or leave a review on the site that you purchased it from.
  id: totrans-153
  prefs: []
  type: TYPE_NORMAL
  zh: 现在您已经完成了使用Python学习模型上下文协议，我们非常乐意听到您的想法！如果您从亚马逊购买了这本书，请[点击此处直接转到该书的亚马逊评论页面](Other_Book_You_May_Enjoy.xhtml)并分享您的反馈或在该购买网站上留下评论。
- en: Your review is important to us and the tech community and will help us make
    sure we’re delivering excellent quality content.
  id: totrans-154
  prefs: []
  type: TYPE_NORMAL
  zh: 您的评论对我们和科技社区非常重要，并将帮助我们确保我们提供高质量的内容。
